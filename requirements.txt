# Astro Runtime includes the following pre-installed providers packages: https://www.astronomer.io/docs/astro/runtime-image-architecture#provider-packages
anthropic
slack-sdk
apache-airflow-providers-slack
apache-airflow-providers-snowflake
snowflake-connector-python
apache-airflow-providers-common-ai[anthropic]
apache-airflow-providers-amazon
sqlglot

# Incident memory: the qbiz-agents RAG engine, installed as a library (see
# RAG_INCIDENT_MEMORY_PLAN.md). Pinned to the feature branch because mcp/mcp_rag is not yet on
# qbiz-agents `master` — it lands via PR #5 (feature/rag-mcp-scaffold). The base package is
# engine-only (numpy + fastembed): no `mcp` runtime in the image. Once PR #5 merges, switch
# `@feature/rag-mcp-scaffold` to `@master` (better: pin a release tag / commit SHA for reproducibility).
qbiz-rag-mcp @ git+https://github.com/Qbizinc/qbiz-agents.git@feature/rag-mcp-scaffold#subdirectory=mcp/mcp_rag
