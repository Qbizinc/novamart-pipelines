# Astro Runtime includes the following pre-installed providers packages: https://www.astronomer.io/docs/astro/runtime-image-architecture#provider-packages
anthropic
slack-sdk
apache-airflow-providers-slack
apache-airflow-providers-snowflake
snowflake-connector-python
apache-airflow-providers-common-ai[anthropic]
apache-airflow-providers-amazon
sqlglot

# Incident memory: the qbiz-agents RAG engine, installed as a library (see
# RAG_INCIDENT_MEMORY_PLAN.md). qbiz-agents PR #5 (feature/rag-mcp-scaffold) has merged to
# master, so this pins to a commit SHA on master rather than a feature branch (a branch can
# move or be deleted out from under this; a SHA can't). Bump the SHA deliberately when the
# engine needs updating. The base package is engine-only (numpy + fastembed): no `mcp` runtime
# in the image.
qbiz-rag-mcp @ git+https://github.com/Qbizinc/qbiz-agents.git@09e1f065cf3098cb5c5f6a0766cf99d0be953049#subdirectory=mcp/mcp_rag

# Harness: code-enforced action caps + audit trail + output validation for the incident-response
# DAG's consequential actions (Slack/Jira/GitHub writes). Pinned to a commit SHA on qbiz-agents
# master for the same reason as qbiz-rag-mcp above.
qbiz-agent-harness @ git+https://github.com/Qbizinc/qbiz-agents.git@557caaf4b135b814f49eb2e28b2d9036d3ffcbf2#subdirectory=harness
